# Stage-1 preprocessing (OSL) on the small MOUS subset.
# Uses existing outputs in data/mous/small/1to50hz_cont to keep this quick.

data_path: "data/mous/raw"
save_path: "data/mous/small/1to50hz_cont"
stage1_path: "data/mous/small/1to50hz_cont"
log_dir: "tmp/examples/flatgpt/preproc/mous/stage1"

delete_fif: false
skip_done: true
gen_report: true

n_workers: 8
use_dask: true

osl_config: configs/preproc/mous/osl.yaml
